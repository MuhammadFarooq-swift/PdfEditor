# 1) Tell pnpm that @embedpdf packages live on the public npm registry
@embedpdf:registry=https://registry.npmjs.org/

# 2) Use auth token if provided, otherwise skip (the "-" makes it optional)
#    This way it won't break if NPM_TOKEN isn't set (like on Vercel).
# //registry.npmjs.org/:_authToken=${NPM_TOKEN-}
